package object.build.domain.root.field;

public enum FieldType {
    string,
    date,
    byte_array,
    long_field,
    double_field,
    boolean_field,
    enum_field,
    enum_list,
    unknown;
}
